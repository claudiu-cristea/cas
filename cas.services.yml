services:
  cas.cas_subscriber:
    class: Drupal\cas\CasSubscriber
    arguments: ['@config.factory', '@logger.factory', '@module_handler', '@path.alias_manager.cached', '@current_user']
    tags:
      - { name: 'event_subscriber' }
    
