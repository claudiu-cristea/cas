services:
  cas.cas_subscriber:
    class: Drupal\cas\CasSubscriber
    arguments: ['@request_stack', '@current_route_match', '@config.factory', '@logger.factory', '@module_handler', '@path.alias_manager', '@current_user', '@path.matcher']
    tags:
      - { name: 'event_subscriber' }
    
